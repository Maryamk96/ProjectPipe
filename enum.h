#pragma once

enum WindowType { MENU_WINDOW, GAME_WINDOW, ABOUT_WINDOW, CLOSE_WINDOW, CURRENT_WINDOW };
enum Direction { UP, LEFT, DOWN, RIGHT, NONE };
enum PipeType { ROW, COLUMN, L1, L2, L3, L4, PLUS }; 
